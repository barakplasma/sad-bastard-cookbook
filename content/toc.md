+++
title = "Table of Contents Placeholder"
template = "toc.html"
# We use the template to generate the real title and content
# Set sort_by if needed, filename usually suffices with numbering
# sort_by = "weight"
# weight = 1 # Ensure it appears early if sorting by weight
+++

This content won't be displayed because the `toc.html` template overrides the content block.